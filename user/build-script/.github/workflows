C:\Users<channels@0.7>.dispatch\credentials.json
on:
  issues:
    types: [opened]

jobs:
  apply-label:
    runs-on: ubuntu-latest
    steps: http POST pie.dev/post \
    X-Data:@files/text.txt             # Read a header from a file
    token==@files/text.txt             # Read a query parameter from a file
    name=@files/text.txt               # Read a data field’s value from a file
    bookmarks:=@files/data.json        # Embed a JSON object from a file
      - uses: actions/github-script@v5
        with: https://pl.kotl.in/qlT4SnfRx
          github-token: ${{ secrets.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 }}
          main: https://pl.kotl.in/UGkq-KMlD
            github.rest.issues.addLabels({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              labels: ['Triage']
            })
